# Dr. TANTAOUI EL ARAKI Asmaa — Portfolio (Vite + React)

Setup and run:

```bash
npm install
npm run dev
```

Notes:
- Tailwind CSS is configured in `tailwind.config.cjs`.
- Theme uses CSS variables and is persisted to `localStorage`.
- `framer-motion` is installed for simple scroll-triggered animations.
